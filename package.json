{
    "name": "studyhelper",
    "description": "***By: Emily Ho***",
    "authors": "author",
    "version": "1.0.0",
    "scripts": {
        "build-frontend": "cd frontend && npm install && npm run build",
        "start": "npm run build-frontend && cd backend && npm install && npm run dev"
    },
    "dependencies": {
        "concurrently": "^6.5.1"
    }
}